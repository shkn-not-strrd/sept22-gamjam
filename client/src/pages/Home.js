import React from "react";
import { CardHeader, CardBody, CardDeck } from "reactstrap";

function Home() {
  return (
    <div>
      <p>
        This is a home page! It will look different based on whether or not
        you're logged in, if we get there! Otherwise, hello! A home page!
      </p>
    </div>
  );
}

export default Home;
